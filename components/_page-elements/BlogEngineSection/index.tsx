import { use } from 'react'
import { BlogEngineSectionI } from '@/definitions/interfaces/_section'

const BlogEngineSection: React.FC<BlogEngineSectionI> = ({
    heading,
    headingFontColor,
    headingFontWeight,
    categoryFilter,
    limit
}) => {
    return (
        <></>
    )
}

export default BlogEngineSection